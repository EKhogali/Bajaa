<?php

/*
|--------------------------------------------------------------------------
| Vendor visibility restrictions
|--------------------------------------------------------------------------
|
| A vendor whose ID appears as a key here is only visible — in the vendor
| list, its transactions, and every vendor/debt report — to the user IDs
| listed in its array. A vendor NOT listed here stays visible to everyone,
| exactly as before this feature existed.
|
| To hide a vendor from everyone except a couple of people, add a line
| like the example below (uncomment and edit the IDs). To lift the
| restriction again, delete the line.
|
| Example:
|   12 => [1, 2],   // vendor #12 only visible to users #1 and #2
|
*/

return [

    // 18 => [1],
    // 19 => [1],
    // 38 => [1],
    // 39 => [1],

];